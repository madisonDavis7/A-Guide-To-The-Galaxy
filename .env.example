# Environment variables for Django deployment
SECRET_KEY=your-secret-key-here
DEBUG=False
PYTHONUNBUFFERED=1

# Database configuration (for Google Cloud SQL or other PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Production configuration
ALLOWED_HOSTS=your-domain.com

# Firebase Configuration (if using Firebase services)
FIREBASE_PROJECT_ID=fnof-stack-a31a1
FIREBASE_PRIVATE_KEY=your-firebase-private-key
FIREBASE_CLIENT_EMAIL=your-firebase-client-email

# Local development only
DJANGO_DATABASE=local
